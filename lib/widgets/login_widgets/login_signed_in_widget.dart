import 'package:flutter/material.dart';

class LoginSignedInWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.check,
      size: 42,
      color: Color(0xFFed576a),
    );
  }
}
